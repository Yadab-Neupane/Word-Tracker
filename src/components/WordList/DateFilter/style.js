
import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        flexDirection: 'column'
    },
    datePickerContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 20
    },
    text: {
        fontSize: 15,
        padding: 3
    }

})
